<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oldstuff.cola.user.mapper.UserAddressMapper">

    <delete id="deleteByIdsAndUserID">
        DELETE FROM public.user_address
        WHERE user_id = #{userID}
        AND id IN
        <foreach collection="idArray"
                 item="id"
                 open="("
                 separator=","
                 close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
